# Organize frontend source by technical layer

Siterm organizes frontend source by technical responsibility—commands, types, hooks, components, styles, configuration, and content—instead of feature-owned vertical slices. This keeps each kind of extension discoverable in one predictable place and preserves a clear dependency direction, accepting that a substantial new command may require coordinated registration across several layers.
